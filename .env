# Copy this to .env and fill in values
# Never commit .env

# App settings
APP_ENV=development
LOG_LEVEL=INFO

# Data paths (relative to project root)
BRONZE_PATH=data/bronze
SILVER_PATH=data/silver
GOLD_PATH=data/gold

# Database
DUCKDB_PATH=data/gold/statsbomb.duckdb

# API
API_HOST=127.0.0.1
API_PORT=8000

# Databricks (V2 — leave blank for local development)
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_CATALOG=
DATABRICKS_SCHEMA=
